
import React from "react";
import ReactDOM from "react-dom/client";
import { IMAGE_URL } from "../../utils/Constants";

const Header = ()=>{
    return (
        <div>
            <div class="logo">
            <img class="w-20 mx-20" src={IMAGE_URL} alt="Logo" />
                
                
            </div>
            <nav>
               heheheh
            </nav>
        </div>
    );
};

export default Header;
